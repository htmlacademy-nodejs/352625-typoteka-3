'use strict';

const PathName = {
  ARTICLES: `articles`,
  MY: `my`,
  REGISTER: `register`,
  LOGIN: `login`,
  SEARCH: `search`,
  ERROR: `error`,
};

const DEFAULT_PORT = 8080;

module.exports = {
  PathName,
  DEFAULT_PORT
};
